---
phase: 01-backend-foundation
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/features/configure/ImagePreview.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Image preview fills available sidebar width without a restrictive max-height"
    - "Magnifier lens is at least 200px diameter with 3.5x zoom for reading fine A5 text"
  artifacts:
    - path: "apps/frontend/src/features/configure/ImagePreview.tsx"
      provides: "Larger image preview and magnifier for A5 index cards"
      contains: "MAGNIFIER_SIZE = 200"
  key_links: []
---

<objective>
Increase image preview and magnifier size for A5 index cards with fine text.

Purpose: UAT test 6 (minor) — the sample card preview in the Configure step sidebar is too small for A5 index cards with fine handwritten text. The magnifier lens (150px, 2.5x zoom) is too small to read the text. Increase preview area, magnifier size, and zoom factor.

Output: Usable image preview that lets users inspect A5 card text through the magnifier.
</objective>

<execution_context>
@/Users/martinhess/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martinhess/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase image preview and magnifier dimensions</name>
  <files>
    apps/frontend/src/features/configure/ImagePreview.tsx
  </files>
  <action>
In ImagePreview.tsx, make the following changes:

1. Increase constants at the top of the file:
   - `MAGNIFIER_SIZE` from 150 to 200
   - `ZOOM_FACTOR` from 2.5 to 3.5

2. On the `<img>` element (line 64), change the max-height constraint:
   - From: `className="w-full max-h-[300px] object-contain rounded"`
   - To: `className="w-full max-h-[500px] object-contain rounded"`

   This lets A5-sized card images display much larger in the sidebar while still being bounded.
  </action>
  <verify>
1. Read ImagePreview.tsx and confirm:
   - MAGNIFIER_SIZE = 200
   - ZOOM_FACTOR = 3.5
   - max-h-[500px] on the img element
  </verify>
  <done>
Image preview and magnifier are sized appropriately for A5 index cards with fine text.
  </done>
</task>

</tasks>

<verification>
- ImagePreview.tsx constants: MAGNIFIER_SIZE = 200, ZOOM_FACTOR = 3.5
- Image max-height is 500px (up from 300px)
- No other components are affected
</verification>

<success_criteria>
UAT test 6 (image preview too small) addressed: preview shows A5 cards at a usable size with a larger, higher-zoom magnifier for reading fine handwritten text.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-09-SUMMARY.md`
</output>
